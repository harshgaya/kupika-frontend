"use client";
import {Search,ChevronLeft,ChevronRight} from "lucide-react";
export function PageHead({title,subtitle,action}){return <div className="page-head"><div><h1>{title}</h1><p>{subtitle}</p></div>{action}</div>}
export function StatCard({label,value,detail,icon:Icon}){return <div className="stat-card"><div className="stat-icon">{Icon&&<Icon size={20}/>}</div><div><span>{label}</span><strong>{value}</strong>{detail&&<small>{detail}</small>}</div></div>}
export function Badge({children,tone="neutral"}){return <span className={`badge ${tone}`}>{String(children||'—').replaceAll('_',' ')}</span>}
export function Toolbar({search,setSearch,children}){return <div className="toolbar"><div className="search"><Search size={17}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name, mobile, order..."/></div>{children}</div>}
export function Pagination({page,pages,onPage}){if(pages<=1)return null;const nums=[];const start=Math.max(1,Math.min(page-2,pages-4));const end=Math.min(pages,start+4);for(let i=start;i<=end;i++)nums.push(i);return <div className="pagination"><span>Page {page} of {pages}</span><div className="page-buttons"><button aria-label="Previous" disabled={page<=1} onClick={()=>onPage(page-1)}><ChevronLeft size={17}/></button>{start>1&&<><button onClick={()=>onPage(1)}>1</button>{start>2&&<span>…</span>}</>}{nums.map(n=><button key={n} className={n===page?'active':''} onClick={()=>onPage(n)}>{n}</button>)}{end<pages&&<>{end<pages-1&&<span>…</span>}<button onClick={()=>onPage(pages)}>{pages}</button></>}<button aria-label="Next" disabled={page>=pages} onClick={()=>onPage(page+1)}><ChevronRight size={17}/></button></div></div>}
export function Empty({text="No data found"}){return <div className="empty">{text}</div>}
