"use client";import {useEffect,useState} from "react";import Link from "next/link";import {Package,MapPin,Truck,ChevronRight} from "lucide-react";import OtpLoginModal from "../auth/otp-login";import {getOrders} from "@/src/lib/api";
export default function OrdersPage(){const[orders,setOrders]=useState([]),[loading,setLoading]=useState(true),[login,setLogin]=useState(false),[error,setError]=useState("");async function load(){setLoading(true);try{setOrders(await getOrders())}catch(e){if(String(e.message).includes('401')||e.message.toLowerCase().includes('verify'))setLogin(true);else setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);return <main className="min-h-screen bg-[#FAF8F4] px-4 py-6 sm:py-10"><div className="mx-auto max-w-3xl"><div className="mb-6 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-emerald-800">My account</p><h1 className="mt-1 text-2xl font-bold text-gray-900 sm:text-3xl">My Orders</h1><p className="mt-1 text-sm text-gray-500">Orders placed on the website, by our team or through the AI assistant.</p></div><Link href="/address" className="hidden items-center gap-1 text-sm font-semibold text-emerald-800 sm:flex"><MapPin size={16}/> Addresses</Link></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}{loading?<div className="rounded-2xl bg-white p-8 text-center text-sm text-gray-500">Loading your orders…</div>:orders.length?<div className="space-y-4">{orders.map(o=><OrderCard key={o._id} order={o}/>)}</div>:<div className="rounded-2xl border border-dashed border-gray-300 bg-white p-10 text-center"><Package className="mx-auto text-gray-400" size={36}/><h2 className="mt-4 text-lg font-semibold">No orders yet</h2><p className="mt-1 text-sm text-gray-500">Your orders will appear here after purchase.</p><Link href="/shop-all" className="mt-5 inline-flex rounded-xl bg-primary px-5 py-3 font-semibold text-white">Start shopping</Link></div>}<Link href="/address" className="mt-5 flex items-center justify-between rounded-2xl bg-white p-4 font-semibold text-gray-800 shadow-sm sm:hidden"><span className="flex items-center gap-2"><MapPin size={18}/> Manage addresses</span><ChevronRight size={18}/></Link></div><OtpLoginModal open={login} onClose={()=>history.back()} onSuccess={()=>{setLogin(false);load()}}/></main>}
function OrderCard({order}){const qty=Number(order.quantity||order.items?.[0]?.quantity||1),unit=Number(order.price||order.items?.[0]?.price||0),total=Number(order.total||unit*qty),status=String(order.status||order.order_status||'placed').toLowerCase(),tracking=order.tracking_id||order.trackingNumber||order.tracking_number||'',provider=String(order.shippingProvider||order.shipping_provider||'').toLowerCase(),date=new Date(order.created_at||order.createdAt).toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'}),image=order.image||order.items?.[0]?.image,product=order.product||order.items?.[0]?.name||'Kupika product',id=`#${String(order._id).slice(-8).toUpperCase()}`;const label=status.replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());return <article className="overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm"><div className="flex items-center justify-between border-b bg-gray-50/70 px-4 py-3"><div><b className="text-sm text-gray-800">{id}</b><p className="text-xs text-gray-400">Placed {date}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold ${status==='delivered'?'bg-green-50 text-green-700':status==='cancelled'?'bg-red-50 text-red-700':'bg-amber-50 text-amber-700'}`}>{label}</span></div><div className="flex gap-3 p-4">{image?<img src={image} alt={product} className="h-20 w-20 shrink-0 rounded-xl bg-gray-100 object-cover"/>:<div className="grid h-20 w-20 shrink-0 place-items-center rounded-xl bg-gray-100"><Package/></div>}<div className="min-w-0 flex-1"><h2 className="truncate font-semibold text-gray-900">{product}</h2><p className="mt-1 text-sm text-gray-500">Qty {qty}</p><p className="mt-1 text-lg font-bold text-gray-900">₹{total.toLocaleString('en-IN')}</p></div></div><div className="border-t px-4 py-3">{tracking?<div className="flex items-center justify-between gap-3 rounded-xl bg-gray-50 p-3"><div className="min-w-0"><p className="text-[11px] font-bold uppercase tracking-wider text-gray-400">{provider==='india_post'?'India Post tracking':'Tracking / AWB'}</p><p className="truncate font-mono text-sm font-semibold">{tracking}</p></div>{provider==='shiprocket'?<a href={`https://shiprocket.co/tracking/${tracking}`} target="_blank" rel="noreferrer" className="shrink-0 rounded-lg bg-gray-900 px-3 py-2 text-xs font-bold text-white">Track</a>:<span className="shrink-0 text-xs font-semibold text-gray-500">Updated by support</span>}</div>:<div className="flex items-center gap-2 rounded-xl bg-blue-50 p-3 text-xs font-medium text-blue-700"><Truck size={16}/> Tracking will appear after dispatch.</div>}</div></article>}
