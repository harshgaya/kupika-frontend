"use client";import {useState} from "react";import DataPage from "@/src/components/admin/DataPage";import CustomerDrawer from "@/src/components/admin/CustomerDrawer";export default function Page(){const[selected,setSelected]=useState(null);return <><DataPage title="Customers" subtitle="Customers created through OTP, checkout, admin and AI calls." endpoint="/api/admin/customers" columns={[{key:'name',label:'Name'},{key:'phone',label:'Mobile'},{key:'createdFrom',label:'Created from'},{key:'createdAt',label:'Joined',render:r=>r.createdAt?new Date(r.createdAt).toLocaleDateString('en-IN'):'—'}]} renderActions={r=><button className="admin-btn secondary" onClick={()=>setSelected(r)}>View</button>}/>{selected&&<CustomerDrawer customer={selected} onClose={()=>setSelected(null)}/>}</>}
